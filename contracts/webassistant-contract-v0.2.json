{
  "schema": "webassistant-contract/v0.2",
  "status": "accepted",
  "accepted": true,
  "conformanceCorpus": "contracts/webassistant-conformance-v0.2.json",
  "observableBoundary": "браузер -> локальная machine-wide служба WebAssistant -> capability рабочей станции",
  "languagePolicy": {
    "documentation": "ru",
    "sourceComments": "ru",
    "technicalIdentifiersMayBeNonRussian": true
  },
  "repositoryModel": {
    "developmentRepository": "netkeep80/WebAssistant",
    "exportRoot": "webassist",
    "exportSemantics": "copy-export-root-contents-to-downstream-repository-root",
    "autonomousAfterExport": true
  },
  "requirements": [
    {
      "id": "WA-REPO-001",
      "kind": "required",
      "statement": "Каталог webassist является автономным переносимым корнем продукта: его содержимое должно собираться, упаковываться и устанавливаться без файлов уровнем выше."
    },
    {
      "id": "WA-REPO-002",
      "kind": "required",
      "statement": "Исходники, build/package/install scripts, runtime configuration, vendored обязательные зависимости и продуктовая документация находятся внутри webassist."
    },
    {
      "id": "WA-CI-001",
      "kind": "required",
      "statement": "Автономный export root содержит собственный .gitlab-ci.yml, который после экспорта работает без файлов родительского development repository."
    },
    {
      "id": "WA-SERVICE-001",
      "kind": "required",
      "statement": "WebAssistant устанавливается администратором один раз на рабочую станцию и работает как machine-wide системная служба."
    },
    {
      "id": "WA-SERVICE-002",
      "kind": "required",
      "statement": "На Windows WebAssistant работает как Windows Service; на ALT Linux — как systemd system service."
    },
    {
      "id": "WA-NET-001",
      "kind": "required",
      "statement": "Browser-facing listener привязывается только к loopback. Текущий default endpoint — http://127.0.0.1:17654; публикация на LAN или 0.0.0.0 запрещена."
    },
    {
      "id": "WA-API-001",
      "kind": "required",
      "statement": "Текущие machine REST operations имеют major-version prefix /v1; production service panel доступна по /."
    },
    {
      "id": "WA-API-002",
      "kind": "required",
      "statement": "Machine REST operations публикуются только под major-version prefix /v1; unversioned aliases, включая /health и /scan, отсутствуют."
    },
    {
      "id": "WA-SCAN-001",
      "kind": "required",
      "statement": "Scanner capability перечисляет устройства, использует детерминированный scannerId selection и при успешном acquisition возвращает один raw application/pdf response."
    },
    {
      "id": "WA-SCAN-002",
      "kind": "required",
      "statement": "POST /v1/scan, /v1/scan/feeder и /v1/scan/duplex передают соответственно glass, feeder и duplex source без скрытого fallback на другой source."
    },
    {
      "id": "WA-SCAN-003",
      "kind": "required",
      "statement": "На рабочей станции одновременно выполняется не более одного physical scanner acquisition; конкурирующий запрос получает conflict и не запускает второй acquisition."
    },
    {
      "id": "WA-TRANSPORT-001",
      "kind": "required",
      "statement": "Успешная scanner operation возвращает документ как binary application/pdf body. Base64, JSON document envelope и ZIP/raster envelope не являются допустимым scanner document transport."
    },
    {
      "id": "WA-SCAN-004",
      "kind": "required",
      "statement": "Один physical acquisition возвращает один PDF, содержащий все страницы этого acquisition. После handoff PDF вызывающей стороне scanner operation заканчивается и не хранит завершённый scan document как long-term scanner storage."
    },
    {
      "id": "WA-SCAN-005",
      "kind": "boundary",
      "statement": "Scanner operation сама не выполняет edit/merge/split PDF, OCR/annotation/watermark/deskew/semantic document transformation, signing/encryption, business/backend upload и не требует business authentication или per-user business profile. Это ограничения scanner capability, а не глобальные запреты WebAssistant; независимые capabilities такого типа требуют отдельного contract/conformance change."
    },
    {
      "id": "WA-DIAG-001",
      "kind": "required",
      "statement": "Versioned diagnostic API возвращает только безопасную runtime-информацию и собственные суточные журналы WebAssistant; произвольный путь к файлу не принимается."
    },
    {
      "id": "WA-LOG-001",
      "kind": "required",
      "statement": "Технические журналы не содержат PDF bytes, Base64 или содержимое страниц/документов."
    },
    {
      "id": "WA-LOG-002",
      "kind": "required",
      "statement": "Технические события WebAssistant записываются в собственные суточные log files; current service сам не выполняет automatic retention/delete старых daily logs."
    },
    {
      "id": "WA-CORS-001",
      "kind": "required",
      "statement": "CORS по умолчанию выключен. При включении разрешены только явно заданные exact HTTP/HTTPS origins; wildcard запрещён."
    },
    {
      "id": "WA-FS-001",
      "kind": "required",
      "statement": "Filesystem capability имеет configured RootDirectory и path resolver, который принимает только относительные пути внутри root и отвергает navigation segments, absolute paths и существующие symlink/reparse-point components."
    },
    {
      "id": "WA-FS-002",
      "kind": "boundary",
      "statement": "В текущем accepted baseline browser-facing filesystem routes не опубликованы; добавление таких routes требует отдельного contract/conformance change и сохранения rooted filesystem boundary."
    },
    {
      "id": "WA-DEPS-001",
      "kind": "required",
      "statement": "Исправленный NAPS2 SDK используется под отдельной repository-owned identity WebAssistant.NAPS2.Sdk и хранится в webassist/vendor/nuget."
    },
    {
      "id": "WA-DOC-001",
      "kind": "required",
      "statement": "Документация о сборке, установке, runtime configuration и current API находится внутри webassist и описывает только фактически существующее поведение."
    },
    {
      "id": "WA-DOC-002",
      "kind": "required",
      "statement": "Новая и изменяемая product/development документация и source comments пишутся на русском языке; технические идентификаторы и термины могут оставаться нерусскими."
    }
  ]
}
